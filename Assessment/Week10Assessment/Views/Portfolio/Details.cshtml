@model Week10Assessment.Models.Asset

<h2>Asset Details</h2>
<div>
    <dl class="row">
        <dt class="col-sm-2">Ticker</dt>
        <dd class="col-sm-10">@Model.Ticker</dd>
        <dt class="col-sm-2">Value</dt>
        <dd class="col-sm-10">@Model.Value.ToString("C")</dd>
    </dl>
</div>
<a asp-action="Index" class="btn btn-secondary">Back</a>